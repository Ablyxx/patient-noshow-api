# patient-noshow-api
A FastAPI-based machine learning web service deployed on Render. It predicts the probability of patient no-shows for medical appointments, serving as the AI inference engine for an automated n8n agentic workflow.
